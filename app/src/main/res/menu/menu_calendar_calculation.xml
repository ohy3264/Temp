<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

  <item
      android:id="@+id/action_kakao_share"
      android:visible="true"
      android:orderInCategory="4"
      app:showAsAction="always"
      android:icon="@drawable/ic_crop_free_white_24dp"/>

  <item android:id="@+id/action_excel"
        android:orderInCategory="3"
        android:icon="@drawable/ic_mail_white_24dp"
        app:showAsAction="always"/>

  <item android:id="@+id/action_month"
        android:orderInCategory="2"
        android:icon="@drawable/ic_assessment_white_24dp"
        app:showAsAction="always"/>


</menu>
